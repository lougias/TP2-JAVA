import java.awt.Image;

/* DynamicThings : éléments mobiles (héros, monstres...) */
public class DynamicThings extends Things {

    public DynamicThings(int x, int y, Image image) {
        super(x, y, image);  // appelle le constructeur Things(x, y, image)
    }
}
